namespace legallead.logging
{
    public enum LogConfigurationLevel
    {
        None = -100,
        Verbose = -1,
        Debug = 0,
        Information = 10,
        Warning = 100,
        Critical = 1000,
        Error = 5000
    }
}